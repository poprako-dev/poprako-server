[workspace]

[package]
name = "sqlx-basic"
version = "0.1.0"
edition = "2024"

[dependencies]
poprako-transactional = { path = "../.." }
async-trait = "0.1.89"
futures = "0.3.32"
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres"] }
tokio = { version = "1", features = ["full"] }
