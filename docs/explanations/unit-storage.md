# Unit 的逻辑与存储

## Unit 在数据库中的形式

Unit 因为需要支持并发修改，所以参考 CRDT，对 unit 仅进行软删除（hide）。这样可以做到无损恢复被并发删除后 save 的操作。
在当前单页的数据不会过于超过 100 的情况下，CRDT 可以显著降低当前版本（7-25）算法的 API IO 和数据库 IO，且不会造成过多的存储消耗。

Unit 在数据库中保持 next_id 维系的单链表结构，由于业务总是 list 整个页的数据，所以在 fetch 的时候没有额外开销，最后只需要在内存中做简单重排即可。
